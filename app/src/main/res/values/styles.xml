<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23" >true</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:textColorPrimary">@color/Black</item>
        <item name="android:textColor">@color/Black</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="toastCorrect">
        <item name="iconLeft">@drawable/ic_check</item>
        <item name="colorBackground">@color/colorAccent</item>
        <item name="textColor">@color/white</item>
        <item name="length">SHORT</item>
    </style>

    <style name="toastWrong">
        <item name="iconLeft">@drawable/ic_clear</item>
        <item name="colorBackground">@color/colorWrong</item>
        <item name="textColor">@color/white</item>
        <item name="length">SHORT</item>
    </style>

</resources>
